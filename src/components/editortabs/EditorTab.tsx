import React from 'react';
import { Box, Typography } from '@mui/material';

export const EditorTab: React.FC = () => {
  return (
    <Box p={2}>
      <Typography variant="h6">Editor Tab</Typography>
    </Box>
  );
}; 