import React from 'react';
import { Box, Typography } from '@mui/material';

export const JsonTab: React.FC = () => {
  return (
    <Box p={2}>
      <Typography variant="h6">JSON View Tab</Typography>
    </Box>
  );
}; 