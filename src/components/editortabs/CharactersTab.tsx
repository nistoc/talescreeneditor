import React from 'react';
import { Box, Typography } from '@mui/material';

export const CharactersTab: React.FC = () => {
  return (
    <Box p={2}>
      <Typography variant="h6">Characters Tab</Typography>
    </Box>
  );
}; 